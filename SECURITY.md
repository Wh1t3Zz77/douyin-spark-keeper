# 安全策略

## 报告安全问题

请优先使用 GitHub 仓库 **Security → Report a vulnerability** 提交私密安全报告。仓库维护者应在首次公开发布前启用 GitHub Private vulnerability reporting。

如果仓库暂未显示该入口，可以只新建一个不含漏洞细节的 Issue，请维护者提供私密联系渠道。不要在公开 Issue 中提交漏洞细节、Cookie、数据库、日志、邮箱、服务器地址或可复现的账号凭据。

报告中请说明受影响版本、部署方式、影响范围和最小复现步骤，并对敏感内容作脱敏处理。维护者确认收到前，请勿公开披露漏洞细节。

## 自托管责任

- 默认仅监听 `127.0.0.1`；开启局域网或公网访问是部署者的显式选择。
- 公网部署必须使用 HTTPS、强随机密钥、邮件验证和防火墙。
- 不要共享 `.env`、SQLite 数据库、Docker 卷、浏览器资料目录或备份。
- 不要将真实用户数据用于公开演示、Issue 或测试夹具。
- 定期更新宿主机、Docker、浏览器运行环境和项目版本。

## 不受支持的行为

本项目不会提供绕过验证码、绕过安全挑战、批量营销或接管未授权账号的能力。
